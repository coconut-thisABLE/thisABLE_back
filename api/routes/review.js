const router = require('express').Router();
// const {} = require('../middlewares/auth');
const controller = require('../../controllers/review');

// router.route('')
//     .post(isAuthenticatedOrAnonymouse(), controller.create);

module.exports = router;
